<%= @extra.foods.map do |food|
  "#{link_to(food.name, edit_admin_restaurant_food_path(@restaurant, food))}
    <sup>#{link_to 'x', admin_restaurant_extra_remove_food_path(@restaurant, @extra, food_id: food.id), method: :post, style: 'color:red !important'}</sup>"
end.to_sentence(last_word_connector: " and ").html_safe %>

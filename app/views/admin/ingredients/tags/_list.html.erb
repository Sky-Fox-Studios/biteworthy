<%= @ingredient.tags.map do |tag|
  "#{link_to(tag.name, edit_admin_tag_path(tag))} <sup>#{link_to 'x', admin_ingredient_remove_tag_path(ingredient, tag_id: tag.id), method: :post}</sup>"
end.to_sentence.html_safe %>

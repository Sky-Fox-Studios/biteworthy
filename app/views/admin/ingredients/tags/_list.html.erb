<%= @ingredient.tags.map do |tag|
  "#{link_to(tag.name, edit_admin_tag_path(tag))} #{link_to Icon.sup_x.html_safe, admin_ingredient_remove_tag_path(ingredient, tag_id: tag.id), method: :post}"
end.to_sentence.html_safe %>

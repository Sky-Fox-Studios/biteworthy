<%= simple_form_for(item, url: admin_restaurant_item_add_new_extra_path(restaurant, item), method: :post, remote: true) do |f| %>
  <fieldset class='inner'>
    <legend>Create</legend>
    <div class="row universal-flex-container no-bottom-margin">
      <div class="small-5 columns">
        <input type="text" id="item[extra_name]" name="extra_name" placeholder="Name" />
      </div>
      <div class="small-7 columns">
        <input type="text" id="item[extra_description]" name="extra_description" placeholder="Description" />
      </div>
      <% Extra.addon_types.each do |addon_type| %>
        <div class="small-5 columns">
          <%= "<input type='radio' name='addon_type' id='addon_type_#{addon_type.first}' value=#{addon_type.first} #{'checked' if addon_type.first == 'addition'}> #{addon_type.first} <br>".html_safe %>
        </div>
      <% end %>
      <div class="small-2 columns">
        <%= button_tag "<i class='fa fa-plus'></i>".html_safe, id: "item-add-new-extra", class: "universal-flex-button flex-button-natural vertical-center no-right-margin" %>
      </div>
    </div>
    <div id="extra-new-notice"></div>
  </fieldset>
<% end %>

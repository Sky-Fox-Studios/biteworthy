<%= simple_form_for(@food, url: admin_foods_path, method: :POST) do |f| %>
  <% if @food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% @food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
     <%= f.association :menu_group, collection: MenuGroup.includes(:restaurant).all.order('restaurants.name').order(:name), as: :select, :label_method => lambda { |menu_group| "#{menu_group.restaurant.name} | #{menu_group.name}" } %>
  </div>
  <div class="field">
    <%= f.input :name %>
  </div>
  <div class="field">
    <%= f.input :description %>
  </div>
  <div class="field">
    <%= f.input :price_low %>
  </div>
  <div class="field">
    <%= f.input :price_high %>
  </div>
  <div class="actions">
     <%= f.submit 'Create' %>
  </div>
<% end %>

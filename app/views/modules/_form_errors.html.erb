<div class="row">
	<div class="small-12 medium-2 large-2 columns center">
      <% if current_model.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(current_model.errors.count, "error") %> prohibited this from being saved:</h2>

	      <ul>
	      <% current_model.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>
  </div>
</div>
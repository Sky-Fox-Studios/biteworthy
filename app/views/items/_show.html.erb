<div class="small-12 medium-6 large-6 columns" style="padding-bottom: 5px">
  <div class="row">
    <div class="small-12 large-6 columns">
      <div class="image-wrapper">
        <%= link_to PhotoDecorator.decorate(item.photos).display, restaurant_item_path(item.restaurant, item) %>
      </div>
      <% if current_user %>
        <div id="rating-reviews-item-<%=item.id%>" class="review-rating-wrapper">
          <% review = Review.where(user_id: current_user.id, review_type: "Item", review_id: item.id).first %>
          <% if review %>
            <%= render partial: "/modules/rating_reviews", locals: { rating: Review.ratings[review.rating], reviewable: item } %>
          <% else %>
            <%= render partial: "/modules/rating_reviews", locals: { rating: 0, reviewable: item } %>
          <% end %>
        </div>
      <% end %>
    </div>
    <div class="small-12 medium-7 large-6 columns">
      <h3 style="float:left;">
        <%= link_to item.name, restaurant_item_path(item.restaurant, item) %>
      </h3>
      <div class="small-12 columns">
        <p align="none" class="white-text">
          <%= item.description %>
        </p>
      </div>
    </div>
  </div>
</div>

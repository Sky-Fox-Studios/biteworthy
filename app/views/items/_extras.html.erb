CHOICE:
  <%= @item.extras.choice.map do |extra|
  "#{link_to(extra.name, edit_admin_restaurant_extra_path(item.restaurant, extra))} #{link_to Icon.sup_x.html_safe, admin_restaurant_item_remove_extra_path(item.restaurant, item, extra_id: extra.id), method: :post}"
end.to_sentence.html_safe %>
<br>
ADDITION:
<%= @item.extras.addition.map do |extra|
  "#{link_to(extra.name, edit_admin_restaurant_extra_path(item.restaurant, extra))} #{link_to Icon.sup_x.html_safe, admin_restaurant_item_remove_extra_path(item.restaurant, item, extra_id: extra.id), method: :post}"
end.to_sentence.html_safe %>
<%= item.extras.map {|extra| link_to(extra.name, restaurant_extra_path(@restaurant, extra)) }.to_sentence.html_safe %>
<br>

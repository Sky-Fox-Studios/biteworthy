<%= PhotoDecorator.decorate(ingredient.photos).display %>
<% if current_user %>
  <div id="rating-reviews-ingredient-<%=ingredient.id%>" class="review-rating-wrapper">
    <%= render partial: "/modules/rating_reviews", locals: { reviewable: ingredient } %>
  </div>
<% end %>

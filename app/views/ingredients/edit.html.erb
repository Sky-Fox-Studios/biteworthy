<div class="row">
	<div class="small-12 columns">
		<h1>Editing Ingredient
      <% if @food %>
        for <%= @food.name %>
      <% end %>
    </h1>
		<%= render 'form' %>
	</div>
</div>

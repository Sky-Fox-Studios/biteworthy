<div class="small-12 columns">
  <h1 class="home-header" align="center">Bite Worthy Durango!</h1>
  <div class="row">
    <% @restaurants.each do |restaurant| %>
      <%= render partial: "restaurants/basic", locals: {restaurant: restaurant} %>
    <% end %>
  </div>
</div>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>BWD</title>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <link href='http://fonts.googleapis.com/css?family=Handlee' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Architects+Daughter' rel='stylesheet' type='text/css'>
    <%= csrf_meta_tags %>
    <%= render :partial => "/ga/tracking" %>
    <script type="text/javascript" src="assets/jscolor.js"></script>
  </head>

   <body>
      <div class="row main-page">
         <div class="small-12 columns">
            <%= render :partial => "/base/header" %>
            <% if flash[:notice] || flash[:alert] %>
              <div class="small-12 columns">
                <% if flash[:alert] %>
                  <div data-alert class="alert-box radius"><%= flash[:alert] %><a class="close">x</a></div>
                <% end %>
                <% if flash[:notice] %>
                  <div data-alert class="alert-box warning radius"><%= flash[:notice] %><a class="close">x</a></div>
                <% end %>
              </div>
            <% end %>
            <%= yield %>

            <%= javascript_include_tag "application" %>
         </div>
         <%= render :partial => "/base/footer" %>
      </div>
   </body>
</html>
